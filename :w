local M = {}

function M.config()
  require'toggleterm'.setup {
    direction = 'float',
    terminal_mappings = false,
    insert_mappings = false,
    float_opts = {
      border = 'curved',
      winblend = 3,
    }
  }
end

return M

